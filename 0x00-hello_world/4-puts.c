#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *puts function to print strings
 */
int main(void)
{
char str1[50];
strcpy(str1, "Programming is like building a multilingual puzzle");
puts(str1);
return (0);
}
