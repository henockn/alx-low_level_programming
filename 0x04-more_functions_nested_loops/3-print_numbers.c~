/**
 * mul - finds multiplication of two numbers
 * @a: the first number
 * @b: the second number
 *
 * Description: multiplication based on arthimetic operator
 * Return:the multiplication of a and b
 */
int mul(int a, int b)
{
return (a * b);
}
